{
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-sass": "^4.1.0",
    "node-sass": "^5.0.0"
  },
  "scripts": {
    "start": "npm run prepare && npm run server",
    "prepare": "gulp prepare",
    "server": "gulp server",
    "build": "gulp build",
    "styles": "gulp styles"
  }
}
